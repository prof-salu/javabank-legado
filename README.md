🏦 JavaBank - Sistema Legado (Módulo 1)Bem-vindo(a) ao repositório oficial do JavaBank. Este projeto é a base prática para o Módulo 1 da disciplina de Padrões de Projetos de Software com Java.📜 O Contexto (A Sua Missão)Você foi contratado(a) como Engenheiro(a) de Software Sénior pelo JavaBank. Infelizmente, a equipa anterior de desenvolvimento não dominava os conceitos de Orientação a Objetos. Eles construíram o núcleo financeiro do banco de forma procedural, colocando toda a lógica de negócio num único método main gigante.Atualmente, o código sofre de:Quebra de Encapsulamento: Qualquer um pode alterar o saldo das contas diretamente.Alto Acoplamento: As classes de negócio estão dependentes de serviços de infraestrutura (como envio de e-mails).Falta de Polimorfismo: O sistema está repleto de blocos if/else e switch/case difíceis de manter.A sua missão é refatorar este código, aplicando os pilares da POO, preparando o terreno para a futura implementação de Padrões de Projeto (GoF).🛠️ Pré-requisitos e SetupAntes de começar a limpar o código, certifique-se de que o seu ambiente está pronto:Java JDK instalado (versão 11 ou superior).VS Code instalado.Extensão Extension Pack for Java instalada no VS Code.Git configurado na sua máquina.Como iniciar:Faça o clone deste repositório para a sua máquina:git clone [https://github.com/SEU-USUARIO/javabank-legado.git](https://github.com/SEU-USUARIO/javabank-legado.git)
Abra a pasta javabank-legado no VS Code.Navegue até ao ficheiro Main.java.Clique no botão de Run (ou pressione F5) para garantir que o sistema compila e executa no terminal.🎯 Laboratórios de RefatoraçãoSiga as instruções abaixo (que também estão na sua Apostila de Laboratório) para consertar o sistema.Laboratório 1: Proteção e Hierarquia (Encapsulamento e Polimorfismo)O código atual altera o dinheiro diretamente e usa if/else para definir as taxas de manutenção.Objetivo: Criar uma classe abstrata Conta com o atributo saldo privado (Encapsulado).Ação: Criar as classes filhas ContaCorrente e ContaPoupanca (Herança) e mover a lógica das taxas de manutenção para dentro delas, eliminando os if/else do Main.java (Polimorfismo).Laboratório 2: Contratos e Desacoplamento (Interfaces)O código atual instancia um ServicoEmail usando a palavra new diretamente dentro da regra de negócio, gerando alto acoplamento.Objetivo: "Programar para interfaces e não para implementações".Ação: Criar uma interface Notificador com o método enviar(). Fazer com que a classe de Conta dependa desta interface e receba a implementação (Email, SMS, etc.) através do seu construtor (Injeção de Dependência).⚠️ Regra de Ouro do RepositórioExiste uma branch neste repositório chamada solucao que contém o código perfeitamente refatorado (o gabarito).NÃO ESPREITE A SOLUÇÃO ANTES DE TENTAR! O processo de aprendizagem em Arquitetura de Software só acontece quando você sente a "dor" de alterar o código legado, comete erros, e tenta aplicar os conceitos por conta própria. O professor mostrará a solução ideal no final da aula.Mãos à obra e boa refatoração! 🚀
